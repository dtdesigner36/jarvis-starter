# Telegram Bot Handlers

**What:** command and message handlers in a Telegram bot (commands, callback queries, inline)
**Where in project:** handlers/ or bot/commands/ depending on structure

→ To walk through: jarvis school topic telegram-bot-handlers
