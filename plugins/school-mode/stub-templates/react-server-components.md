# React Server Components (RSC)

**What:** components rendered on the server, reduce client JS bundle
**Where in project:** all components in app/ are RSC by default; `'use client'` makes a client component

→ To walk through: jarvis school topic react-server-components
